# Central env template (copy fragments into each service dashboard)
# UI-side (Vite will expose VITE_* at build time)
VITE_APP_NAME=Mareekh
VITE_ENV=staging
VITE_API_BASE=https://your-backend-url

# Backend stub
APP_NAME=mareekh-backend-stub
APP_ENV=staging
LOG_LEVEL=INFO

# Auth (placeholder for future Keycloak/OIDC)
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_AUDIENCE=

# Always inject via platform env; don't commit real secrets.
