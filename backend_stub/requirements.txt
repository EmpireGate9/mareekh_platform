fastapi==0.114.2
uvicorn[standard]==0.30.6
pydantic==2.8.2

# مكونات اختيارية لكن موصى بها
python-multipart==0.0.9       # لدعم رفع الملفات
sqlalchemy==2.0.34            # لإدارة قاعدة البيانات (اختياري)
httpx==0.27.2                 # للطلبات الخارجية (اختياري)
